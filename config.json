{
    "model" : {
        "metadata_path" : "data/metadata.csv",
        "data_path"     : "data",

        "architecture"  : "vgg16",
        "input_size"    : [224, 224],
        "labels"        : ["bus", "car", "motorcycle", "taxi", "truck"]
    },

    "train" : {
        "batch_size"       : 12,
        "learning_rate"    : 1e-5,
        "num_epochs"       : 100,
        "over_epochs"      : 60,
        "metrics"          : ["accuracy"]
    }
}
